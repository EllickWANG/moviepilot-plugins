# AI字幕自动生成(接口ASR)

使用线上 ASR 接口从视频音轨生成带时间轴的原文字幕，再复用私有版大模型接口配置翻译中文字幕。

该插件是独立新插件，不依赖本地 faster-whisper，不下载本地识别模型，语音识别只走线上接口。

## 功能特点

- 支持外挂字幕、内嵌字幕、音轨 ASR 的字幕提取优先级
- 音轨识别使用远程 `/v1/audio/transcriptions` 接口
- 默认 ASR 模型为 `whisper-1`，需要接口支持 `verbose_json` 和 `segments`
- 音频按分钟分段提取，分段关闭后立即上传 ASR，识别完成后删除临时分段
- 处理过程中会随机抽检音频分段和 ASR 返回时间轴
- 默认复用 `AI字幕自动生成(v2) 私有版` 的接口地址、密钥和翻译模型
- 支持使用大模型将原文字幕翻译成中文字幕
- 支持定时扫描媒体路径，新完整视频自动加入队列
- 支持任务队列、后台运行、进度页和仪表盘刷新，小组件进度条只展示正在处理任务
- 配置页按运行、处理、接口和高级参数重新分区，任务页提供后台扫描、失败重试和历史清理按钮
- ASR 接口请求期间会强制写入等待秒数，避免长请求时小组件看起来不刷新
- 支持 ASR 分段和字幕翻译断点续跑，意外中断后复用已成功的部分
- 批量翻译使用 JSON 编号回填，并在接口返回异常时自动拆分批次重试
- 支持配置翻译接口并发数，默认 1，可按接口稳定性调高
- 视频不完整时进入等待状态，到期后重新检查，不计为失败

## 配置说明

### 基础配置

| 配置项 | 说明 | 默认值 |
| --- | --- | --- |
| 启用插件 | 是否启用插件 | 否 |
| 发送通知 | 是否发送任务执行通知 | 否 |
| 媒体入库自动执行 | 监听媒体入库事件并加入队列 | 是 |
| 定时扫描媒体路径 | 按 cron 周期扫描媒体路径 | 是 |
| 扫描周期(cron) | 定时扫描周期 | `*/10 * * * *` |
| 不完整重试(分钟) | 文件不完整时等待后重试 | 10 |
| 并行任务数 | 同时处理任务数量，接口不稳定时建议谨慎调高 | 1 |
| 翻译接口并发数 | 同时发起的翻译请求数量，接口不稳定时建议保持 1 | 1 |
| 翻译成中文 | 生成原文字幕后翻译中文字幕 | 是 |

任务页提供一次性操作按钮：立即扫描、重试失败、清理已完成、清理历史。这些操作不再作为常驻开关显示在配置页。

### 接口ASR配置

| 配置项 | 说明 | 默认值 |
| --- | --- | --- |
| 允许从音轨生成字幕 | 无外挂/内嵌字幕时允许调用 ASR | 是 |
| 自动检测语言 | 不使用音轨元数据，由 ASR 接口识别语言 | 是 |
| 复用私有版接口配置 | 复用 `AI字幕自动生成(v2) 私有版` 的接口地址、密钥、代理和翻译模型 | 是 |
| ASR接口模型 | 语音识别模型，必须返回时间轴 segments | `whisper-1` |
| 音频分段分钟 | 每段音频上传时长 | 10 |

### 大模型接口配置

默认复用 `AI字幕自动生成(v2) 私有版` 的接口配置。关闭复用后，可在当前插件内单独配置：

| 配置项 | 说明 | 默认值 |
| --- | --- | --- |
| 使用代理服务器 | 是否使用 MoviePilot 代理访问接口 | 否 |
| 兼容模式 | 是否避免自动拼接 `/v1` | 否 |
| OpenAI API URL | 接口地址 | `https://api.openai.com` |
| API密钥 | 接口密钥 | 空 |
| 翻译模型 | 翻译中文字幕使用的大模型 | `gpt-4o-mini` |

### 翻译参数

| 配置项 | 说明 | 默认值 |
| --- | --- | --- |
| 每批翻译行数 | 每批处理的字幕行数，范围 1-50，建议 20-25 | 20 |
| 翻译接口并发数 | 同时发送的翻译批次数，接口不稳定时保持 1 | 1 |
| 上下文窗口大小 | 翻译时参考的上下文行数 | 5 |
| LLM请求重试次数 | 翻译失败时的重试次数 | 3 |
| 翻译英文时合并整句 | 英文字幕先合并整句再翻译 | 否 |
| 完整解码校验 | 使用 ffmpeg 完整解码扫描视频，高 CPU，默认关闭 | 否 |

## 字幕提取策略

字幕提取优先级：外挂字幕 > 内嵌字幕 > 接口 ASR。

音轨 ASR 只负责生成原文字幕和时间轴；中文字幕由翻译流程生成。这样比直接让音频大模型输出中文字幕更稳定，时间轴也更可控。

音频处理采用流水线方式：ffmpeg 写完一个分段后，插件立即上传该分段到 ASR 接口，识别完成后删除临时音频文件。运行中只会保留正在写入的最后一个分段和少量待处理分段，不会等待整部视频全部提取完成后再开始识别。

## 注意事项

1. ASR 接口必须支持 `/v1/audio/transcriptions`、`verbose_json` 和 `segments`，否则无法稳定生成 SRT 时间轴。
2. 当前线上测试可用的稳定 ASR 模型为 `whisper-1`。
3. 插件不会下载或加载本地语音识别模型。
4. 随机抽检使用 ffprobe 读取分段元数据，并检查 ASR 返回的 segments 时间轴是否递增。
5. 定时扫描不会重复触发已完成、已忽略、处理中或已在队列中的视频。
6. 翻译全部失败时不会标记任务完成，也不会生成全是“翻译失败”的中文字幕。
7. 批量翻译会要求模型返回带 id 的 JSON；如果模型未按格式返回，插件会自动拆分批次重试，最后才退到单行。
8. ffmpeg 相关处理固定使用 1 个线程，降低 CPU 峰值。
9. 插件关闭时会中断当前 ffmpeg 音频提取；接口请求会在超时或返回后结束。
